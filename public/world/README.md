# Рабочие изображения мира

Основная карта — `maps/forest-region-v3.png`: просторный лесной регион 1254 × 1254. По просьбе пользователя карта полностью переработана: домашняя поляна стала небольшим участком среди лесов и тропинок, пещера находится слева сверху, большой берег — справа снизу. Это изменение масштаба нарисованных объектов, а не увеличение числа пикселей файла.

SHA-256: `6bc7d8274bc1f1e660de570f0ac4d9eb1f7651ad1a38a84abba9e5b01c45be59`.

Кнопка и открытый мир используют одну геометрию: фиксированный квадрат `(486, 514, 256, 256)`. Для него добавлена детальная текстура `maps/home-detail-v1.png` (1254 × 1254), созданная встроенным imagegen по точному вырезанному участку. Общий подготовленный слой 1024 × 1024 рисуется и в кружке, и в мире. Наружные 2 единицы участка сохраняют исходную карту, следующие 12 плавно вводят детали. Это защищает границу от квадратного стыка; новая картинка уточняет рисунок внутри, а не меняет масштаб и привязки.

Логические координаты анимаций остаются 256 × 256. Canvas кружка учитывает DPR экрана (до 1024 пикселей), отдельный canvas в мире всегда 1024. Основная карта учитывает DPR до 2 с пределом 2200 пикселей по длинной стороне. Фон уменьшается с качественной фильтрацией, пиксельные спрайты рисуются без сглаживания.

Дополнительная текстура не блокирует базовую сцену: при ошибке загрузки остаётся первоначальная карта и работающий персонаж. Поздняя загрузка после закрытия сцены её не возобновляет. Фонарь сохраняет нарисованный корпус и стекло, вечером добавляется только мягкий радиальный свет без непрозрачной закраски.

SHA-256 детального PNG: `f14fba0d71b976ed63530726e3b4a86241ab72a409e8e837a7a5b36fae5ef8bd`. Точный запрос: `docs/game/home-detail-prompt.txt`.

Мир открывается примерно в пять раз дальше домашнего вида. Кнопка «Вся карта» возвращает полный обзор, кнопка с прицелом — подробный домашний участок. Минимальное увеличение вписывает карту целиком даже на вертикальном экране; свободное место по длинной стороне нормально. При таком обзоре маленькая метка куста скрывается, чтобы не перекрывать метку дома.

| Файл | Назначение |
|---|---|
| `maps/forest-region-v3.png` | Общий фон региона и резерв домашней сцены |
| `maps/home-detail-v1.png` | Детальный домашний участок с прежней геометрией |
| `maps/forest-home-v2.png` | Предыдущая утверждённая карта, сохранена для истории |
| `routes/trail.webp`, `routes/river.webp` | Иллюстрации прежних экспедиций |
| `maps/forest-expanded.webp`, `maps/home-clearing.webp` | Прежние фоны; активная сцена их не загружает |
| `buildings/house-details.webp`, `buildings/workshop-atlas.webp` | Прежние накладки; несовместимы с новым фоном и не загружаются сценой |

## Привязки

Все исходные координаты находятся в `features/world/map-manifest.ts`: дверь, подход, фонарь, куст и его перекрывающая листва, пещера, водная область, безопасная зона движения и участок будущих улучшений дома. `map-layout.ts` переводит координаты между миром и домашним фрагментом, выполняет проверку попадания в полигоны. Камера не меняет эти данные.

На пещеру можно нажать целиком. Вся вода открывает «Рыбалку»; вогнутый полигон следует изгибам берега и исключает лесистые полуострова. Обе локации пока показывают «В разработке» и возвращают к прежнему положению камеры.

Контракт будущего оформления дома и проверки описаны в `docs/game/map-assets-plan.md`. Изображение нельзя незаметно заменить другим рисунком с прежним именем: новая геометрия требует новой ревизии манифеста.
