# Новости версий и меню уровней

[Документация](../README.md) · [Разделы приложения](app-features.md) · [Игровые правила](backend-and-data.md)

## Где менять

| Задача | Файл |
|---|---|
| Опубликовать запись об обновлении | [public/updates.json](../../public/updates.json) |
| Текст о сотрудничестве, окно и кнопка beta-test | [beta-info.tsx](../../features/check-in/beta-info.tsx), [beta-info.module.css](../../features/check-in/beta-info.module.css) |
| Формат записей и ключ хранилища прочитанного | [release-notes.ts](../../features/updates/release-notes.ts) |
| Проверка сервера, таймеры, прочитанное и обмен между вкладками | [release-notes-store.ts](../../features/updates/release-notes-store.ts) |
| Связь состояния с React | [use-release-notes.ts](../../features/updates/use-release-notes.ts) |
| Подключение к текущему аккаунту | [check-in-app.tsx](../../features/check-in/check-in-app.tsx), компонент `BetaInfo` |
| Группы уровней, прогресс и этапы значков | [game-levels-button.tsx](../../features/game/game-levels-button.tsx), [game-levels.module.css](../../features/game/game-levels.module.css) |
| Пороги уровней и смены значка | [clicker-story.ts](../../features/game/clicker-story.ts), `CLICKER_LEVELS`, `CLICKER_ICON_LEVELS` |
| Рисунок и цвет значка | [game-level-icon.tsx](../../features/game/game-level-icon.tsx) |

## Добавить обновление

1. Добавьте запись в `public/updates.json → releases`. `schemaVersion` оставьте равным 1. Пример полей:

   ```json
   {
     "id": "2026-10-01-forest-update",
     "version": "0.6.4",
     "date": "2026-10-01",
     "title": "Новые возможности леса",
     "changes": ["Короткое описание видимого игроку изменения."]
   }
   ```

2. Укажите реальную версию и дату выпуска. Новые записи ставьте в начало массива; при одинаковой дате сохраняется их порядок. `id` уникален и остаётся неизменным после публикации; старые записи сохраняйте. Новое отдельное объявление получает новый ID, даже если номер версии прежний. Исправление опечатки в существующей записи не делает её непрочитанной снова.
3. Пишите о доступном игроку результате. Не копируйте коммиты, внутренние пути и результаты CI в список изменений. Не объявляйте отключённую механику работающей.
4. Проверьте `node --test tests/release-notes.test.mjs tests/beta-info.test.mjs`; перед выпуском — обычные обе сборки и тесты. Убедитесь, что `/updates.json` отдаёт JSON с новой записью из развёрнутой версии.
5. Выпустите вместе с соответствующим кодом по [инструкции эксплуатации](../operations/operations.md). Сам push или изменение `package.json` не создаёт новость на основном сервере.

Пример выше — формат, а не запланированный выпуск. Начальная запись текущей ветки использует версию пакета 0.6.3.

Формат: 1–200 записей; `id` — до 100 символов из латинских букв, цифр, `.`, `_`, `-` (начинается с буквы/цифры); `version` — до 40, `title` — до 160 символов; `changes` — 1–12 непустых пунктов до 400 символов каждый. Дата должна существовать в календаре. Текст обычный, без HTML. Некорректный файл отклоняется целиком, на экране остаётся последний корректный список.

## Как работает счётчик

- Приложение загружает `/updates.json` при запуске, возвращении во вкладку/сеть и периодически, пока вкладка видна. Открытие окна также проверяет сервер. Новость может появиться без перезагрузки страницы.
- Файл публичный, не содержит персональных данных. Он обслуживается веб-приложением; путь `/api/*` здесь не используется, поскольку на VPS его принимает Ktor.
- `fetch` использует `cache: no-store`; [next.config.ts](../../next.config.ts) и [public/_headers](../../public/_headers) задают отсутствие кеширования для двух контуров. Service Worker не перехватывает запрос новости. Встроенная копия даёт начальное содержимое и работает при недоступной сети; ошибка запроса не удаляет уже загруженный список.
- Счётчик — число записей с непрочитанными ID. При открытии окна раскрывается и отмечается последняя запись. Остальные отмечаются только при раскрытии; новая запись, пришедшая в уже открытое окно, сама прочитанной не становится.
- Прочитанные ID сохраняются **для аккаунта в браузере этого устройства**, синхронизируются между вкладками. На другом устройстве запись может снова быть новой; серверной синхронизации прочитанного пока нет. При недоступном хранилище просмотр работает до конца сеанса.
- Новости не перезагружают приложение, не очищают данные и не трогают очередь игровых тапов. Обнаружение записи не означает, что уже открытая вкладка исполнила новый JavaScript.

При изменении формата сохраняйте совместимость со старыми открытыми клиентами. Ключи и политику хранения ищите в модуле записей; ID нельзя заменять индексом массива или номером последней просмотренной версии.

## Как устроено меню уровней

Всего 100 уровней, первый начинается с 0 тапов. Группы без пересечений: **1–10, 11–20, …, 91–100**. При открытии выбирается группа текущего уровня; одновременно показывается до десяти строк. Переход между группами не меняет игровой прогресс.

Полоса сверху показывает путь от текущего порога до следующего по **подтверждённым** тапам. На 100-м уровне отображается завершение, без несуществующего следующего уровня. Не считайте долю как `lifetimeTaps / nextThreshold`: предыдущие уровни уже пройдены; используйте `getClickerLevelProgress`.

Этапы смены значка берутся отдельно из `CLICKER_ICON_LEVELS`: **1, 10, 20, 30, 40, 50, 60, 75, 90, 100**. Замок показывает, что этап ещё не получен, но не мешает посмотреть его требования. Группы по десять не должны менять эти пороги.

Проверка UI: маленький экран, длинные числа, текущие уровни 1/10/11/75/100, повторное открытие после просмотра другой группы, клавиатура и возврат фокуса. Тесты: [game-levels.test.mjs](../../tests/game-levels.test.mjs). Изменение самого начисления прогресса описано в [правилах и данных](backend-and-data.md#изменить-уровни-или-добавить-достижение).
