# Требования «Я живой»

## Основа 0.1

1. При первом входе человек вводит только имя.
2. Сервер создаёт внутренний UUIDv7, случайный публичный ID `XXXX-XXXX-XXXX` и сессию устройства. Повтор потерянного bootstrap-запроса с тем же ключом возвращает ту же личность.
3. Имя не уникально и не участвует в формировании ID.
4. Главный экран содержит одну доминирующую кнопку с неизменным текстом **«Я ЖИВОЙ»**.
5. Успех показывается только после ответа сервера. Время берётся из PostgreSQL.
6. Каждое допустимое нажатие создаёт отдельное append-only событие.
7. Bootstrap и check-in требуют `Idempotency-Key`. Новая отметка допускается раз в 30 секунд; повтор ключа не создаёт пользователя или событие заново.
8. Без отметки кнопка серая. За 0–24 часа цвет плавно идёт от зелёного через янтарный к красному. После 24 часов остаётся красным.
9. Цвет всегда сопровождается точным текстом; красный цвет не означает экстренную ситуацию.
10. При отсутствии сети отметка не отправляется и не помещается в offline-очередь.

## Изменения 0.4.4

1. Копирование ссылки приглашения и системная отправка являются отдельными действиями. Нажатие **«Скопировать»** не открывает системное окно **«Поделиться»**, а отправка не запускает параллельную запись в буфер обмена.
2. Полная ссылка приглашения отображается в доступном только для чтения поле. При недоступности Clipboard API приложение выделяет это поле и выполняет совместимое с Safari синхронное копирование; пользователь всегда может скопировать выделенную ссылку вручную.
3. Локальный HTTP в LAN не считается подтверждённым современным Clipboard API результатом: интерфейс различает подтверждённое копирование, legacy-fallback и ошибку, не показывая ложный успешный статус.
4. Состояние окна приглашения хранит открытость отдельно от режима `link` / `qr`. Закрытие меняет только открытость и сохраняет QR-режим до завершения exit-анимации.
5. Регрессионные тесты проверяют полный LAN URL, независимость копирования и системной отправки, а также сохранение QR-содержимого при закрытии.

## Изменения 0.4.3

1. Во время сюжета под единственной сюжетной фразой остаётся одна компактная строка времени последней серверной отметки. Без активного сюжета эта строка становится основным статусом.
2. Возраст подтверждённой отметки переводится в цвет непрерывной функцией на всём интервале 0–24 часа: зелёный → янтарный → красный. Клиент считает возраст от последнего `serverTime`, периодически обновляет цель цвета и плавно интерполирует переход; после 24 часов цвет остаётся красным.
3. Уровень 1–10 зависит от общего числа локально сохранённых тапов на границах 0, 10, 25, 50, 100, 250, 500, 1 000, 2 500 и 5 000. Лучшая непрерывная серия не влияет на уровень и хранится отдельно. Формат v3 безопасно мигрирует данные v1/v2.
4. В профиле показываются общее число тапов, текущий уровень, сколько осталось до следующего, и процентная полоса. На главной эти числа не дублируются.
5. Первый тап новой серии принимается только круглой кнопкой и, если серверный cooldown уже закончился, запускает серверную отметку. После этого и до 30-секундного timeout вся игровая область принимает каждый touch pointer; она сама не имеет права начать новую серию или повторить API-запрос.
6. Экран **«Личные связи»** разделён на внутренние вкладки **«Люди»** и **«Группы»**. Ошибка загрузки одного раздела не скрывает другой.
7. Во вкладке людей доступны три явных способа приглашения: точный ID, одноразовая ссылка и QR той же ссылки. Ссылка хранится в PostgreSQL только как SHA-256 hash, действует до 7 дней, принимается одним человеком и не раскрывает старые отметки; выпуск новой ссылки немедленно отзывает предыдущую.
8. В профиле владелец заранее назначает до трёх активных подтверждённых личных контактов для восстановления. Браузер, в котором пользователь возвращает доступ, создаёт попытку на 10 минут и отправляет другу approval-ссылку. Друг может только выбрать заранее доверившийся ему профиль и одобрить попытку; сама ссылка не является credential для входа.
9. Завершить одобренную попытку может только исходный браузер с отдельной `HttpOnly; SameSite=Strict` cookie (`__Host-zhiv_recovery` в production, `zhiv_recovery` в локальной разработке). Completion возвращает прежний `app_users.id`, отзывает старые сессии целевого профиля и случайную сессию, из которой было начато восстановление, но не объединяет данные двух профилей. Токены приглашения и одобрения находятся во fragment URL, сразу убираются из адресной строки и передаются API только в JSON body; секрет исходного браузера остаётся в cookie. В PostgreSQL сохраняются только SHA-256 hashes.
10. `/healthz` проверяет жизнь процесса, `/readyz` — доступность PostgreSQL. CI обязан выполнить web, Kotlin, PostgreSQL integration и production Compose smoke checks.

## Игровой UX 0.4.2

1. Счётчик активной серии содержит число тапов, оставшиеся целые секунды и градиентную полосу времени. Полоса начинается полной после каждого тапа и достигает нуля ровно через 30 000 мс бездействия.
2. Информативный таймер остаётся видимым при `prefers-reduced-motion`; отключаются только декоративные анимации.
3. На touch-устройстве вся игровая область между шапкой и нижним меню принимает касания. Каждый `touch` pointer, включая non-primary, даёт один локальный тап; совместимый Safari-click не создаёт второй.
4. Мышь и клавиатурная активация основной кнопки сохранены. Касания шапки, ID и нижнего меню не относятся к игре.
5. Геометрия кнопки и экрана не меняется: обычный тап использует только transform/opacity-отклик, а завершение серии — отдельный короткий импульс без прокрутки и layout shift.
6. Просроченная серия, найденная при первичной загрузке, не проигрывает декоративный взрыв. Эффект возникает только при живом завершении на открытом экране или при возвращении в уже открытое приложение.
7. Рубежи от 50 получили разные эффекты: `50` — искры, `100` — общий праздник, `500` — орбиты, `1 000` — кометы, `10 000` — легендарный финал, `100 000` — чемпионский эффект.
8. Промежуточный успешный текст о проверке сервера не показывается: первый тап сразу открывает сюжет. Ошибки сети и offline-состояние по-прежнему видимы.
9. В профиле игровая метрика называется **«лучшая серия»**. Слово «стрик» относится только к ежедневным дням и не используется для рекорда тапов.
10. Тумблер личного доступа имеет различимые track и thumb, положение слева/справа, текст `Включено`/`Выключено`, `aria-label` и `aria-describedby`.

## Сюжетная мини-игра 0.4.1

Каждый физический тап сразу увеличивает текущую локальную серию. Если с последнего тапа прошло 30 000 мс, серия завершается и счётчик становится нулевым; следующий тап начинает новую серию с единицы. Личный рекорд обновляется на каждом тапе, хранится отдельно для публичного ID и никогда не уменьшается. Одна серия ограничена 100 000 тапами.

Первый тап новой серии может создать одну серверную отметку. Остальные тапы активной серии обрабатываются локально — независимо от server cooldown, ответа API или ошибки сети. Серверное состояние не имеет права менять таймер, счёт, рекорд или сюжет игры.

Доступны 24 сюжета. Один сюжет закрепляется за серией и меняется только после её завершения. Порядок детерминированно перемешан seed публичного ID и до обхода всего каталога не повторяется. Каждый сюжет содержит десять сцен:

| Тап | Поведение |
|---:|---|
| 1 | начало сюжета сразу после локального тапа |
| 5 | новая сцена и конфетти |
| 10 | новая сцена |
| 20 | новая сцена и световые кольца |
| 50 | новая сцена и искры |
| 100 | новая сцена, конфетти, кольца и свечение |
| 500 | новая сцена и орбитальные кольца |
| 1 000 | новая сцена и кометный эффект |
| 10 000 | легендарная сцена, конфетти и усиленное свечение |
| 100 000 | чемпионский финал |

Между рубежами сюжетный текст не меняется. Лучшая серия следует этим сюжетным рубежам, а уровень с 0.4.3 вычисляется отдельно из общего числа тапов на более короткой шкале. На главном экране нет следующей сцены, прогресс-бара сюжета, постоянных наград, аудитории и общего числа отметок; внутри счётчика остаётся только таймер текущей серии. Игровые уровень и рекорд находятся в профиле. Звук и вибрация выключены; `prefers-reduced-motion` отключает только декоративные эффекты.

После завершения серии клиент best-effort отправляет один `CLICKER_SERIES_FINISHED` на `/api/v1/game-events`. UUIDv4 создаётся при первом тапе, переживает reload вместе с серией и используется только для bounded дедупликации; в лог он не попадает. Endpoint требует сессию, trusted origin, строгий JSON до 2 КБ и rate limit; значения помечаются `client_reported=true`, не сохраняются в PostgreSQL и не влияют на продуктовые данные. На каждый тап телеметрия не отправляется.

Отдельно сервер считает только принятые отметки. Долговечный текст меняется редко: на 10, 20, 50, 100, 200, 500, 1000 отметках и далее через каждую тысячу. Локальные тапы не увеличивают серверный `checkInCount`, повтор `Idempotency-Key` возвращает исходный порядковый номер.

## Скользящая серия отметок и профиль

1. Сервер строит максимальные последовательности принятых `check_ins`, в которых соседние отметки разделены не более чем 24 часами. Часовой пояс, локальная дата и полночь в расчёте не участвуют.
2. Ровно 24 часа сохраняют серию; превышение хотя бы на микросекунду начинает новую. Точный дедлайн `renewBy` всегда равен последней принятой отметке плюс 24 часа.
3. Повторные отметки продлевают дедлайн, но сами по себе не накручивают дни. Длина последовательности равна единице плюс числу полных 24-часовых интервалов от её начала до последней отметки.
4. После истечения `renewBy` текущая серия равна нулю, исторический рекорд сохраняется. Клиент обновляет состояние после отметки, при возврате в приложение и сразу после серверного дедлайна.
5. Вкладка **«Профиль»** показывает имя, публичный ID, ежедневный стрик и неактивное место будущей аватарки.
6. Имя не уникально, нормализуется по тем же правилам, что onboarding, и меняется через `PATCH /api/v1/me` не чаще одного раза за скользящие 24 часа.
7. Первое изменение после создания или миграции доступно сразу. Сохранение того же нормализованного имени не расходует интервал.
8. Изменение имени не меняет публичный ID, cookie-сессии, связи, группы, аудитории или историю отметок.
9. В базе заранее предусмотрен приватный `avatar_storage_key`; клиенту он не выдаётся, загрузка изображения в 0.4.0 не входит.

## Люди и группы

- Экран **«Личные связи»** разделяет личные связи и группы внутренними вкладками **«Люди»** и **«Группы»**.
- `DIRECT` — отдельная связь ровно двух людей, например с девушкой.
- `GROUP` — семья, друзья или произвольная группа с ролями `OWNER`, `ADMIN`, `MEMBER`.
- Поиск человека только по точному публичному ID; обе стороны подтверждают связь.
- Приглашение в группу адресовано конкретному подтверждённому личному контакту и живёт 7 дней.
- Отметка глобальная: пользователь нажимает кнопку один раз. Получатели фиксируются снимком в `check_in_audiences`, чтобы новый участник не увидел чужую старую историю.

## Версия 0.2 · личные связи

1. Пользователь делится публичным ID и ищет другого человека только по точному ID.
2. Связь появляется только после принятия заявки второй стороной. Заявку можно отклонить или отменить; она истекает через 7 дней.
3. Один пользователь не может добавить себя, создать вторую активную связь с тем же человеком или открыть связь третьему участнику.
4. Во вкладке **«Люди»** видны входящие заявки, исходящие заявки и принятые люди.
5. Для каждого человека владелец отдельно выбирает: показывать или скрывать свои новые отметки.
6. Выключение доступа немедленно скрывает доступную отметку. Повторное включение не открывает события, созданные до включения.
7. Отметка до принятия связи никогда не становится видимой после принятия.
8. Удаление архивирует связь для обеих сторон. Повторное добавление создаёт новый период без доступа к прежней истории.
9. Главный экран до нажатия показывает число людей, которым будет доступна новая отметка.
10. Клиент обновляет личные связи каждые 30 секунд, при возврате в окно/PWA и после восстановления сети.

## Версия 0.3 · группы

1. Пользователь создаёт до 20 активных групп с названием и необязательным emoji; начальные пресеты — семья, друзья и произвольная группа.
2. Пригласить можно только человека из активной подтверждённой личной связи. Личная связь не означает согласия на группу: membership появляется только после принятия адресного приглашения.
3. Владелец переименовывает, приглашает, отзывает приглашения, удаляет участников и архивирует группу. Обычный участник может выйти, но не удалить владельца или группу.
4. Владелец группы неизменяем; передача владения не входит в 0.3.
5. У каждого участника отдельная настройка **«Показывать через эту группу»**. Источник истины — `circle_sharing_preferences`, а не membership-флаг.
6. Отметка до вступления, во время выключенного доступа или после выхода не открывается. Повторное вступление создаёт новый membership-период.
7. Последняя отметка участника читается только через `check_in_audiences` конкретной группы и текущий membership получателя.
8. Удаление группы закрывает активные membership-периоды и приглашения, затем архивирует круг; исторические строки не удаляются.
9. Создание группы и отдельное приглашение идемпотентны. Максимум — 50 активных участников и ожидающих приглашений вместе.
10. Число получателей следующей отметки считается по уникальным людям через личные связи и группы.

## Версия 0.3.5 · мобильный UX

1. Поле публичного ID принимает ввод с дефисами и без них. Дефисы расставляются автоматически, а вручную введённый разделитель после четвёртого и восьмого символа не исчезает.
2. Поиск человека остаётся точным поиском по публичному ID. Поиск по имени используется только локально среди уже подтверждённых связей в окне создания группы.
3. Основное меню **«Я живой» / «Люди»** остаётся доступным независимо от длины списка; прокручивается содержимое вкладки, а не весь экран.
4. Копирование ID участника работает через Clipboard API и совместимый с Safari fallback для локального HTTP.
5. Фильтрация людей по имени не сбрасывает уже выбранных получателей приглашения.

## Версия 0.3.6 · плавающее мобильное меню

1. В мобильной компоновке меню выводится поверх содержимого одной компактной капсулой; полноширинной нижней панели, линии или подложки нет.
2. Меню остаётся неподвижным при прокрутке, а содержимое визуально проходит под полупрозрачным стеклом.
3. Для Safari используются blur, saturation и `-webkit-backdrop-filter`; без поддержки blur включается контрастный полупрозрачный fallback.
4. Учитывается `env(safe-area-inset-bottom)`, а последний интерактивный элемент можно полностью прокрутить выше меню.
5. Активная вкладка выделяется движущейся линзой. Сохраняются семантический `nav`, `aria-current`, focus ring, touch-target не меньше 48 px и reduced motion.
6. На широких экранах меню остаётся отдельной строкой существующей компоновки.

## Версия 0.3.7 · iPhone polish

1. На телефонах overlay меню ограничен шириной стеклянной капсулы и находится внутри экранной оболочки. Полноширинного compositing-слоя, линии или перекрытия снизу нет.
2. Меню остаётся неподвижным внутри оболочки высотой `100dvh`, пока содержимое активной вкладки прокручивается независимо.
3. В мобильной и touch-компоновке системные полосы прокрутки внутренних контейнеров скрыты стандартным свойством и WebKit fallback. Прокрутка пальцем и momentum scrolling сохраняются.
4. На устройствах с мышью сохраняется тонкая полоса прокрутки для длинных списков.
5. Safe area, запас под последнюю карточку, порядок наложения диалогов, семантика `nav`, focus ring и `aria-current` остаются без изменений.

## Версия 0.3.8 · touch polish и нейминг

1. У мобильного стеклянного меню нет внешней тени: Safari не получает прямоугольную область, которую можно обрезать у нижнего края viewport. Внутренние блики и граница капсулы сохраняются.
2. Next.js dev indicator скрыт для чистой проверки на телефоне; production-поведение не меняется.
3. Текст главной кнопки нельзя выделить долгим нажатием. Для кнопки и её содержимого отключены `user-select` и iOS touch callout.
4. Viewport фиксирует масштаб `1`, а корень приложения и внутренние scroll-контейнеры разрешают только `touch-action: pan-y`. Pinch zoom и многопальцевый сдвиг отключены, однопальцевая вертикальная прокрутка работает.
5. Нижняя вкладка называется **«Люди»**. На её экране используется один семантический `h1` **«Личные связи»** без дублирующего заголовка **«Свои»**.
6. Safe area, focus ring, `aria-current`, диалоги, переключатели и доступность последнего элемента списка сохраняются.

## Версия 0.3.9 · приватные периоды и сюжетный кликер

1. Для личной связи API явно возвращает состояние отметки: `HIDDEN`, `WAITING_INITIAL`, `WAITING_AFTER_REENABLE` или `AVAILABLE`.
2. `WAITING_INITIAL` означает, что после принятия связи ещё не было доступной отметки. `WAITING_AFTER_REENABLE` означает, что доступ включили повторно и теперь ожидается новая отметка.
3. Состояние вычисляется по `circle_sharing_preferences.created_at`, текущему `enabled_since` и доступному audience snapshot. Точный момент переключения клиенту не раскрывается.
4. Повторное включение доступа не открывает старую отметку, а появление следующей разрешённой отметки переводит состояние в `AVAILABLE`.
5. Первая сюжетная механика 0.3.9 заменена непрерывными сериями 0.4.1; актуальные рубежи, уровни, сброс и компоновка главного экрана описаны выше.

## Сессии и восстановление

- Единственный долгоживущий credential в браузере — `Secure; HttpOnly; SameSite=Lax` cookie; сырой токен не попадает в БД или логи.
- Незавершённый idempotency key и его неизменный payload временно хранятся в `sessionStorage` с TTL, чтобы безопасно пережить reload после оборванного ответа.
- Один пользователь может иметь несколько сессий устройств.
- В профиле можно назначить не более трёх recovery-контактов. Подходит только активная подтверждённая `DIRECT`-связь; отзыв доверия, удаление связи или её последующее пересоздание делают старое разрешение недействительным.
- Восстановление начинается в браузере, где пользователь хочет вернуть доступ: до onboarding либо из случайно созданного профиля. Этот исходный браузер попытки создаёт пару независимых секретов: approval-token для ссылки другу и claim-secret, который сервер устанавливает только ему в короткоживущую `HttpOnly; SameSite=Strict` cookie (`__Host-zhiv_recovery` в production, `zhiv_recovery` в локальной разработке).
- Ссылка вида `/#/recover/<approval-token>` действует 10 минут и даёт аутентифицированному другу только preview заранее доверившихся ему активных профилей и право одобрить один из них. Она не выдаёт сессию и не позволяет завершить вход на устройстве друга или у перехватившего ссылку человека.
- Исходный браузер опрашивает свою попытку и после одобрения явно вызывает completion. Сервер повторно проверяет активность доверия, атомарно отзывает прежние сессии целевого профиля и случайную initiating-сессию, все recovery-контакты кроме одобрившего, все незавершённые direct-invite ссылки, исходящие запросы дружбы и групповые приглашения цели, а также ещё не завершённые восстановления, ранее одобренные скомпрометированным профилем для других людей. Затем claim-secret повышается до новой обычной сессии того же `app_users.id`, а попытка помечается завершённой. Повтор с тем же `Idempotency-Key` безопасно возвращает ту же сессию: потерянный HTTP-ответ не создаёт новый credential и не блокирует восстановление.
- Approval-token и claim-secret хранятся в PostgreSQL только как 32-байтные SHA-256 hashes. Попытка одноразовая, живёт 10 минут и не продлевается preview или polling.
- Восстановления по имени или публичному ID нет: это позволило бы украсть личность.
- Следующий этап — passkey/Face ID, recovery codes и управление устройствами. Администратор группы восстанавливает членство, но не личность.

## Потом

- календарь: был ли check-in в конкретный локальный день и список времён;
- выбор аудитории и настройки приватности;
- passkeys и управление устройствами;
- opt-in уведомления с quiet hours, cooldown, deduplication и digest;
- экспорт/удаление данных.

## Не входит в MVP

Геолокация, фоновой трекинг, чат, звонки, медицинские или экстренные обещания, автоматическая тревога, публикация номера телефона и восстановление через поддержку «на доверии».

## Приёмка 0.2

- профиль переживает перезагрузку страницы;
- зелёный цвет появляется только после подтверждённого ответа;
- два параллельных check-in дают одно событие и один cooldown;
- повтор одного idempotency key возвращает то же событие;
- `/api/**` не выдаётся из PWA cache;
- имя из одного символа допустимо, пробелы нормализуются, управляющие символы отклоняются;
- клавиатура, focus ring, screen reader status и reduced motion работают;
- миграции проходят на настоящем PostgreSQL 18.
- одна заявка отображается как исходящая у отправителя и входящая у получателя;
- повтор запроса с одним `Idempotency-Key` не создаёт дубликат;
- принять заявку может только получатель, отменить — только отправитель;
- событие до связи, во время выключенного доступа и из архивной связи недоступно;
- список людей читает время только через `check_in_audiences`, а не из чужого профиля.

## Приёмка 0.3

- повтор создания с одним ключом возвращает одну группу;
- приглашение невозможно без подтверждённой личной связи;
- принять приглашение может только адресат;
- отметка до вступления не появляется после принятия;
- OFF скрывает отметку, повторный ON не возвращает старую;
- вышедший или удалённый участник не получает новые снимки аудитории;
- одинаковый получатель через `DIRECT` и несколько `GROUP` считается один раз;
- `check_ins` и `check_in_audiences` не допускают UPDATE/DELETE;
- manifest, системный share и главная кнопка используют бренд **«Я живой»**.

## Приёмка 0.3.5

- `4HTD-` не теряет дефис, а `4HTDXTP7` отображается как `4HTD-XTP7`;
- лишний тринадцатый символ не обрезается до другого валидного ID;
- copy-only действие не открывает системное меню «Поделиться» и использует fallback на iPhone/LAN HTTP;
- нижнее меню не меняет положение при прокрутке длинного списка людей и групп;
- поиск группы нечувствителен к регистру, пробелам и различию `е`/`ё`;
- выбранный человек остаётся выбранным после фильтрации и очистки поиска.

## Приёмка 0.3.6

- на мобильном viewport видна только компактная стеклянная капсула без нижней полосы;
- меню не движется при прокрутке длинного списка;
- содержимое видно под стеклом, а последняя карточка и её действия доступны выше меню;
- safe area iPhone/PWA не перекрывает кнопки;
- без поддержки blur меню остаётся контрастным и читаемым;
- активная линза плавно переезжает между вкладками и отключает движение при `prefers-reduced-motion`;
- клавиатурный focus и `aria-current` работают для обеих вкладок.

## Приёмка 0.3.7

- на реальном iPhone в портретной ориентации и установленной PWA вокруг меню нет полноширинного шва или прямоугольного перекрытия;
- при вертикальной прокрутке справа не появляется полоса прокрутки;
- список продолжает плавно прокручиваться пальцем, а последняя карточка и её действия доступны выше меню;
- меню остаётся неподвижным, учитывает safe area и не перекрывает открытые диалоги;
- на широком экране тонкая полоса прокрутки остаётся доступной.

## Приёмка 0.3.8

- под стеклянной капсулой нет прямоугольной тени или визуальной нижней полосы;
- в локальном dev-режиме не показывается круглый индикатор Next.js;
- долгое нажатие на **«Я ЖИВОЙ»** не выделяет текст и не вызывает iOS callout;
- жест двумя пальцами не масштабирует и не сдвигает интерфейс, а список продолжает прокручиваться одним пальцем;
- нижняя вкладка подписана **«Люди»**, экран содержит единственный заголовок **«Личные связи»**;
- последняя карточка, диалоги, ввод ID и все кнопки остаются доступными.

## Приёмка 0.3.9

- первая пустая личная связь показывает **«После добавления ещё не отмечался»**;
- выключенный доступ показывает **«Отметки недоступны»**, а повторно включённый — **«Ждём новую отметку после включения доступа»**;
- после повторного включения старая отметка остаётся скрытой, новая доступная отметка снова показывает фактическое время;
- исторические требования к кликеру 0.3.9 заменены приёмкой 0.4.1 ниже; требования приватности остаются действующими.

## Приёмка 0.4.1

- первый тап серии равен 1; на 29 999 мс серия активна, ровно на 30 000 мс завершается, а следующий тап снова равен 1;
- серия 651 ждёт сцену 1 000, а не 655; сюжет не меняется ни на одном рубеже и переключается только после timeout;
- личный рекорд обновляется сразу, не уменьшается после слабой попытки и ограничен 100 000;
- ровно 24 сюжета используют рубежи 1, 5, 10, 20, 50, 100, 500, 1 000, 10 000 и 100 000 без ранних повторов;
- старый v1 `totalTaps` не становится текущей или лучшей серией; v2 восстанавливает незавершённую серию только до истечения 30 секунд;
- ошибка, cooldown или поздний ответ API не сбрасывает и не воскрешает игровую серию;
- на iPhone координаты кнопки и нижнего меню не меняются при быстрых тапах; главный экран не прокручивается, а вкладки людей и профиля прокручиваются;
- главная содержит только огонёк ежедневного стрика, кнопку, текущую серию и одну сюжетную фразу; игровой рекорд и уровень доступны в профиле;
- завершение серии создаёт не более одного агрегированного privacy-safe события; ошибка телеметрии не влияет на отметку или игру;
- историческая календарная модель этого релиза заменена скользящим серверным окном 0.4.3;
- профиль открывается третьей кнопкой стеклянного меню и имеет собственную прокрутку над safe area;
- первое изменение имени доступно сразу, второе до истечения 24 часов получает `429 DISPLAY_NAME_COOLDOWN` и `Retry-After`;
- повтор того же имени не расходует cooldown, а повтор ключа с другим payload получает конфликт;
- после переименования публичный ID, сессия, связи, группы и история отметок сохранены;
- поля будущей аватарки мигрируют как `NULL`, storage key никогда не выходит в публичный API;
- web lint, TypeScript, production build, domain/PWA/telemetry tests и backend tests проходят; интеграционный backend-тест допускает skip только без Docker.

## Приёмка 0.4.2

- на 0, 15 000 и 30 000 мс после тапа таймер показывает соответственно 100%, 50% и 0%; новый тап снова даёт 100%;
- касание двумя и более пальцами внутри игровой области увеличивает серию на число касаний, но запускает не более одного check-in-запроса;
- touch-generated click не удваивает тап, а мышь, клавиатура и assistive activation основной кнопки продолжают работать;
- обычные тапы, рубежи 50+ и завершение серии видимы, не меняют layout и отключаются при `prefers-reduced-motion`; сам таймер при этом не исчезает;
- эффект завершения не воспроизводится при холодном восстановлении старой серии и не отправляет дополнительную телеметрию;
- первая сюжетная фраза не перекрывается текстом о проверке сервера; сетевые ошибки остаются видимыми;
- профиль показывает ровно одну **«лучшую серию»** тапов вместо максимума ежедневных дней, а огонёк продолжает показывать текущую ежедневную серию;
- тумблер личного доступа имеет заметный светлый бегунок и дублирует положение словами;
- два тапа с одинаковым `updatedAtMs` сохраняют больший актуальный счёт после merge и reload;
- lint, TypeScript, обе production-сборки, web-тесты и backend-тесты проходят; реальный multi-touch окончательно проверяется на iPhone.

## Приёмка 0.4.3

- последняя отметка видна и без активного сюжета, и отдельной короткой строкой при сюжете; сетевое сообщение не выдаётся за подтверждённое время;
- оттенок кнопки монотонно и без дискретных суточных ступеней проходит контрольные точки 0, 6, 12, 18 и 24 часа и остаётся красным позже;
- серия активна на точной границе `+24h`, истекает после неё, не зависит от timezone и сохраняет исторический максимум;
- уровень меняется от `lifetimeTaps`, слабая серия не снижает уровень, а серия 100 000 не ограничивает дальнейший общий счёт;
- на пустой серии касание вне кнопки ничего не делает; после первого тапа два touch pointer в игровой области дают два локальных тапа и не создают второй check-in;
- вкладки людей и групп имеют корректные `tablist/tab/tabpanel`, работают стрелками и не скрывают группы при ошибке людей;
- Link и QR содержат один и тот же URL; self, повторный redeem, использованная и истёкшая ссылка имеют терминальный понятный ответ;
- токены не присутствуют в URL HTTP-запроса, access-log и строках БД; в БД находятся только 32-байтные hashes;
- попытку восстановления создаёт только браузер, в котором пользователь возвращает доступ; approval-ссылка живёт 10 минут, показывает другу только заранее доверившиеся ему активные профили и сама никогда не выдаёт сессию;
- одобрить выбранный профиль может только назначенный активный `DIRECT`-контакт; отзыв доверия, удаление или пересоздание связи блокируют preview, confirm и ещё не завершённую попытку;
- завершить одобренную попытку может только браузер с исходной recovery-cookie (`__Host-zhiv_recovery` в production); завершение возвращает прежний ID и историю, отзывает старые сессии целевого профиля и initiating-сессию случайного аккаунта, не перенося его данные;
- повтор start, approve или complete с тем же `Idempotency-Key` не создаёт вторую попытку, одобрение или сессию; другой payload с тем же ключом конфликтует;
- тумблер остаётся на одной строке с подписью на 320–520 px и имеет читаемые состояния словами;
- `/readyz` отвечает `503` при недоступной БД; CI не допускает тихого skip PostgreSQL integration suite.
