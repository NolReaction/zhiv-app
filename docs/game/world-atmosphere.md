# Дождь, вода и жизнь леса

[Навигация по документации](../README.md) · [Устройство мира](world-foundation.md) · [Свет](world-lighting.md) · [Tiled](tiled-editor.md)

Эта страница описывает текущие `forest-*` модули новой карты. Декоративные эффекты и сценки не дают наград и не отправляют команды на сервер.

## Где менять

| Задача | Файл и точка изменения |
| --- | --- |
| Погода и интенсивность дождя | `features/world/forest-atmosphere.ts`: `WEATHER_PRESETS`, `forestAtmosphereState` |
| Длина, наклон, скорость, прозрачность падающих капель | `features/world/forest-rain.ts`: `sampleForestRain`, `drawForestRain` |
| Форма кругов, всплеска и отскока на воде | Там же: `drawForestWaterImpact` |
| Границы реки и исключения | `world/tiled/forest.tmj`: `Water`, `WaterExclusions` |
| Плотность/распределение попаданий на реке и блики течения | `features/world/forest-water.ts`: `layout`, `forestWaterFrame`, `drawForestWater` |
| Брызги на земле | `features/world/forest-ground-impacts.ts`: `forestGroundImpactFrame`, `drawForestGroundImpact` |
| Участки земли для брызг | `features/world/forest-ground-layout.json`; проверка совместимости в `groundLayout` |
| Существующие лужи и высыхание | `features/world/forest-ground-weather.ts`: `updateForestWetness`, `forestGroundWeatherFrame` |
| Виды, группы, маршруты, посадки птиц | `features/world/forest-birds.ts`: `SCENARIOS`, `PERCHES`, `forestBirdFrame` |
| Реакция сидящих птиц на шаги и куст | `features/world/forest-bird-reactions.ts`; события — `forest-director.ts` |
| Рисунок птиц, бабочек и светлячков | `features/world/forest-wildlife.ts` |
| Насекомые, территории, посадки и контакт | `features/world/forest-fauna.ts`, `hero-anchors.ts`; Tiled: `Habitats`, `WildlifeAnchors` |
| Выбор занятия, подход и согласование прерываний | `features/world/forest-director.ts`; выбор мест прогулки — `forest-behavior.ts` |
| Грибы, листик и фазы предметной сценки | `features/world/forest-life.ts`; рисунок — `forest-life-painter.ts` |
| Общие часы и ручные события двух видов | `features/world/forest-session.ts`; подключение в `new-map-scene.ts` |
| Переключатели и кнопки DEV | `features/world/dev/world-dev-store.ts`, `world-dev-panel.tsx` |

Старые `water-ambience.ts`, `bird-ambience.ts`, `weather-visitors.ts` и `features/mochlik/insects.ts` относятся к прежней сцене. Для текущей карты начинайте с таблицы выше.

## Настроить дождь

Состояния погоды: `clear` → 0, `drizzle` → 0.35, `rain` → 0.68, `downpour` → 1. В `auto` действует декоративный 24-минутный цикл с длинными сухими промежутками и плавной моросью максимум 0.42; это не прогноз погоды. Облачность как отдельный режим отсутствует.

Воздушные капли, река и земля — три отдельных эффекта с общей интенсивностью. Меняйте нужный модуль, чтобы настройка всплеска не меняла весь дождь. `FOREST_RAIN_LIMIT` ограничивает воздушные капли 180; автоматический режим использует более редкий дождь. Часть капель распределена около `focus`, остальные — по миру, а не по экрану камеры.

Дождь задаётся через **DEV → Погода**. Переключатель луж в DEV управляет их видимостью; влажность накапливается постепенно и высыхает медленнее. Для появления луж оставьте дождь включённым на некоторое время. Новые лужи пока не добавляем: в `forest-ground-weather.ts` остаются до четырёх небольших луж около точки героя.

## Подключить или поправить воду

Разметьте поверхность полигоном в **Water**, а листья/камни, на которых не нужны круги, — в **WaterExclusions**. Пошаговые правила, группы и допустимые имена: [Вода и исключения в Tiled](tiled-editor.md#вода-и-исключения).

`forest-water.ts` делит воду на участки примерно 36 единиц при размере мира 1254. Каждый участок получает два независимых потока попаданий. Заранее подбираются безопасные точки: внутри одного всплеска центр неподвижен, после завершения выбирается другой. Поэтому узкий рукав не зависит от случайного попадания нескольких глобальных точек в широкую реку.

Подробные контуры Tiled не упрощаются. При подготовке сцены проверяется **весь максимальный след анимации**, включая мелкие исключения внутри него; повторной обрезки каждого штриха нет. В узких местах возможны меньшие круги. Максимум — 640 одновременных слотов попаданий и 38 бликов; фактическое число зависит от площади, погоды и фазы.

Если увеличиваете радиус/разлёт в `drawForestWaterImpact` или `forestWaterFrame`, одновременно пересмотрите границы в `footprintFits` и отступы проверки видимости. Иначе ранее безопасная точка позволит новому рисунку выйти на берег. Без `Water` речных эффектов нет.

## Поправить брызги на земле

Земля получает краткий влажный след, приплюснутый всплеск и 2–4 капли, без водных колец. Место и вариант выбираются заново для каждого события. Выключение луж не выключает эти брызги.

`forest-ground-layout.json` содержит проверенные ячейки травы/почвы для конкретного URL изображения с `?v=`. `groundLayout` дополнительно исключает текущие водные поверхности, водные исключения и здания; грибы передаются отдельными динамическими исключениями. Пространство вне воды автоматически безопасной землёй не считается: там могут быть кроны, мост или крыша.

После замены фона **не подменяйте только хеш** в JSON. Перепроверьте безопасные ячейки по новому рисунку; готовой команды повторной генерации маски в репозитории сейчас нет. При несовпадении изображения брызги на земле отключаются, чтобы не рисоваться в случайных местах. Контуры реки продолжают работать по Tiled независимо от этой маски. Если меняете размер брызги, пересмотрите `FOOTPRINT`.

## Добавить сценарий или вид птиц

Сейчас есть четыре вида: зяблик, зарянка, синица и ласточка. Шесть сценариев включают одиночный визит, пары с посадкой, стаю, погоню и перелёт между кронами. В группе максимум пять птиц, ручной визит завершается за 34 секунды. **DEV → Сценарий с птицами** перебирает варианты.

- Состав группы, вид и длительность задавайте в `SCENARIOS`; траекторию и фазы посадки/чистки/взлёта — в `forestBirdFrame` и `perchVisit`.
- Новый внешний вид добавляйте в тип `ForestBirdSpecies` и `drawForestBird` в `forest-wildlife.ts`.
- Точки `PERCHES` стоят на проверенных ветвях/кронах; `forestBirdPerches` принимает только конкретный фон `VERIFIED_TERRAIN` и исключает места рядом со зданиями. После изменения рисунка проверьте ноги сидящей птицы и границы дома. При несовместимом фоне сохраняются пролёты без посадок.
- Начало и конец маршрута должны находиться за границами **мира** с запасом на весь силуэт. Не начинайте DEV-сценарий в `focus`. Птица может влететь в текущий экран из соседней части большой карты.
- `birdSeed` и `birdStarted` хранятся в общей сессии: два вида не должны запускать два разных визита по одной кнопке.

Точки посадки и сценарии пока не авторятся в Tiled. Автоматический режим учитывает день/ночь и дождь; принудительное включение доступно через DEV. «Меньше движения» убирает птиц.

Шаги Мохлика и толчки куста передаются как локальные события. `forest-bird-reactions.ts` выбирает одну ближайшую сидящую птицу: она ненадолго отвлекается от чистки, поворачивает голову и хвост. Её ID, маршрут и самостоятельный взлёт сохраняются; новая птица для реакции не создаётся. Паузы между реакциями защищают от непрерывного вздрагивания.

## Бабочки, светлячки и Мохлик

Позициями всех насекомых владеет `forest-fauna.ts`. В общей сессии каждая особь имеет постоянный `id`, размер, фазу крыльев, территорию, скорость, состояние, цель и паузу после встречи. В текущей карте `Habitats` распределяет 6 бабочек и 12 светлячков: 3 и 5 соответственно возле полянки, остальные 3 и 7 — в широких лесных территориях. `WildlifeAnchors` задаёт реальные листья для отдыха и укрытия. [Редактирование территорий и посадок](tiled-editor.md#территории-и-посадки-насекомых).

Обычный полёт сохраняет спокойные орбиты; отдых, подлёт, отлёт и возвращение используют непрерывное движение. Возврат направлен к текущей точке орбиты, а не к старой позиции до встречи. В сумерках бабочки ищут укрытие, а светлячки становятся активны; дождь ограничивает встречи и отправляет особей к размеченной листве. Они не удаляются из популяции. `shelter` отмечает достаточно широкий участок растения для нескольких особей, `rest` — отдельную посадку. Без подходящей особи кнопка встречи сообщает отказ, а не создаёт партнёра у лапы.

Один `encounter` в `forest-fauna.ts` резервирует героя и `entityId`. Фазы: заметить → поднять лапу → подлететь → сесть → отпустить. Посадка требует достижения мировой точки лапы с небольшой скоростью; `hero-anchors.ts` учитывает позу, размер, точку ног, подъём и сжатие тела. При тапе герой освобождается за короткое завершение до 0.6 с, а та же особь продолжает отлёт и индивидуальную паузу. Повторный тап не перезапускает её траекторию.

`faunaRenderFrame` отдаёт один снимок всей популяции, включая сидящего партнёра. `forest-wildlife.ts` рисует бабочек до ночного освещения, светлячков после него. Светлячок сохраняет тельце, крылья, компактное свечение и индивидуальную фазу до/во время/после контакта; `resting: true` складывает крылья. `forest-life-painter.ts` рисует предметы и не создаёт второго насекомого. Все координаты мировые и одинаковы в обеих камерах.

`forest-director.ts` выбирает реальную встречу или доступный предмет, помнит недавние действия и оставляет спокойные паузы. `forest-life.ts` ведёт грибы, листик и завершение обращения с предметом. Листик лежит на проверенной земле возле spawn, поднимается и возвращается обратно. Грибы растут в точках `Mushrooms`; для подбора нужен выросший гриб и путь к безопасной точке опоры перед ним. Недоступные грибы остаются декорацией. `clearing-activity.ts` исполняет движение, `forest-behavior.ts` выбирает места осмотра, обнюхивания, ухода и отдыха. [Настройка полянки](tiled-editor.md#свободная-полянка-и-места-занятий).

Локальные шаги и шевеление куста могут ненадолго отвести несколько свободных насекомых в сторону. Импульс меняет цель/скорость; участник встречи не отвлекается и не клонируется. Состояния отдыха и встреч меняются только в симуляции, рисование их не продвигает.

Куст относится к маршрутам, а не к предметной сценке: его фазы и события встряхивания задаёт `clearing-activity.ts`, перекрытие тела и движение листвы — `forest-bush-painter.ts`. Траектории декоративных ягод и листика, отскоки и время исчезновения — `forest-bush-particles.ts`; частицам передаётся остановимый таймер сценки, а не время погоды. Разметка находится в `Bushes` и `Routes`; [как её редактировать](tiled-editor.md#куст-контур-укрытие-и-подход).

Для просмотра водной геометрии используйте **DEV → Постройки и разметка → Границы воды**. Голубой контур — поверхность воды, коралловый пунктир — исключения. Это отдельный переключатель, не требующий включать дождь или остальные границы.

## Общие правила и проверка

Все эффекты используют активное время общей сессии. Не добавляйте собственный `setInterval`, `Date.now()` или `Math.random()` в рисование: круг и карта должны показывать одинаковую фазу, а пауза — останавливать событие. Скрытая вкладка не «догоняет» пропущенные минуты. Reduced motion отключает дождевые частицы/птиц и замораживает текущие позиции насекомых и контакт. Явный тап сначала снимает привязку к лапе с сохранением позиции особи; её полёт продолжится после возобновления движения. Создатели фауны и поведения принимают seed для воспроизводимых проверок; отдельного поля seed в DEV пока нет.

```bash
node --test --test-concurrency=1 tests/forest-atmosphere.test.mjs tests/forest-rain.test.mjs tests/forest-water.test.mjs tests/forest-ground-impacts.test.mjs tests/forest-birds.test.mjs tests/forest-life.test.mjs tests/new-map-scene.test.mjs
node --test tests/forest-fauna.test.mjs tests/navigation.test.mjs tests/clearing-navigation.test.mjs tests/living-world-debug.test.mjs
npm run typecheck
```

Ручная проверка: морось/ливень, оба рукава реки, открытая земля/крыша/причал, переключение круг ↔ карта, день/ночь, пауза, «Меньше движения», повтор DEV-сценария. Включите **Особи и их цели** и проследите ID до посадки на лапу и после отлёта; повторите тап, сон героя и дождь во время встречи. **Проходимость и цель Мохлика** помогает проверить путь и отказ для недоступного предмета. Водные и земляные попадания имитируют дождь; каждая летящая капля не отслеживается до отдельного столкновения с поверхностью.
