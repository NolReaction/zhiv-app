# Как работать с проектом

Начните с [состояния работы](WORK_STATE.md) и [путеводителя](docs/repository-guide.md). Рабочую ветку проверяйте через `git branch --show-current` и текущую задачу; не переключайтесь на прежнюю ветку из старого чата. В `master` попадает отдельно проверенный релиз.

## Небольшое изменение

1. Проверьте `git status --short`, затем обновите согласованную рабочую ветку через `git pull --ff-only`.
2. Запустите `npm run dev:local`. Для телефона в той же сети — `npm run dev:lan`.
3. Найдите нужный раздел в `features/`. Компонент и его `.module.css` лежат рядом.
4. Измените одну законченную вещь. Проверьте её на узком и широком экране; для графики также проверьте день и ночь.
5. Перед коммитом выполните `npm run typecheck` и подходящие тесты. Перед релизом нужны обе сборки, полный набор веб-тестов и CI API.
6. Посмотрите `git diff --stat` и `git diff`: в коммит должны попасть только относящиеся к задаче файлы. Обновите `WORK_STATE.md` и сохраните этап обычным новым коммитом. В файле состояния оставьте результат, проверки и следующий шаг; подробную историю храните в Git.

## Картинки и стили

- Иконки достижений: `public/achievements/`. Шесть — SVG, `full_collection.png` — PNG. Серый вид до получения задаёт CSS, отдельная чёрно-белая копия не нужна.
- Редактируемые PNG мира: `art/world/`; браузерные изображения: `public/world/`. [Каталог и порядок ручного экспорта](art/README.md).
- Рисунок Мохлика: `features/mochlik/pixel-sprite.ts`. Это программный пиксельный спрайт; его движения задаются отдельно.
- Новое изображение добавляйте в папку его назначения. Основная игра использует `features/world/art.ts`; каталог прототипа задаётся в `world/tiled/forest.tmj` и проверяется `npm run world:export`. Концепты и запросы для генерации — в `docs/game/concepts/`.
- Совпадение размера файлов недостаточно: у уровней дома должны совпадать точка опоры, вход, масштаб пикселя и освещение.

## Правила порядка

- Новый игровой компонент и его стиль кладите в соответствующий раздел `features/`. Общие элементы — в `components/`, общие функции — в `lib/`.
- Внутри раздела используйте `./file`; между разделами — `@/features/...` или `@/lib/...`.
- В `public/` лежат только используемые приложением файлы и их краткие описания. Удаляя ассет, проверяйте и динамические пути: имя файла может собираться из выбранного маршрута или ID достижения.
- История версий хранится в Git. Не добавляйте ZIP-копии проекта, `node_modules`, сборки и файлы `final-v2-copy`.
- Инструкции эксплуатации — `docs/operations/`, игра — `docs/game/`, текущий релиз — `docs/releases/`. Завершённые отчёты и старые спецификации доступны в истории Git по ссылке из `docs/README.md`; не дублируйте их в рабочем дереве. В `docs/history/` оставлена инструкция восстановления старого VPS checkout.
- Секреты и реальные пользовательские данные не попадают в Git. Существующие миграции БД не переписывайте: изменение схемы оформляется следующей миграцией.
- Библиотечные `components/ui/`, конфигурацию сборки и скрипты деплоя меняйте по конкретной необходимости. Локальный API и настоящий Ktor должны одинаково понимать игровые команды.
