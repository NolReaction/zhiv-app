# Текущее состояние работы

- Репозиторий `NolReaction/zhiv-app`, ветка `feature/mochlik-tiled-world`, продолжение `9184744`. Этап: обратная связь, периоды статистики, управляемое обновление открытого приложения. Master и production не изменять без задания.
- `public/updates.json` намеренно пуст. Парсер принимает 0–200 записей; новая запись добавляется вручную по `docs/development/updates-and-levels.md`. Новость и смена сборки — разные механизмы.
- В beta-test добавлена форма «Написать разработчику»: ошибка/предложение/другое, 10–3000 Unicode-символов. Черновик и точный незавершённый запрос сохраняются в sessionStorage отдельно по аккаунту; повтор не создаёт дубль. Проверка ожидаемого владельца защищает от смены сессии в соседней вкладке.
- Ktor: `feedback/*`, `JdbcFeedbackRepository.kt`, V29. Лимит 1 успешное обращение за скользящие24ч, транзакционная блокировка аккаунта, UUID-квитанции, авторизация и origin, дополнительный HTTP rate limit. Все категории поступают в новую вкладку админки; email не используется. Фильтры, страницы25, статусы new/reviewed/resolved. Merge переносит сообщения/лимит, удаление аккаунта удаляет тексты. Локальная имитация — в памяти; admin остаётся только Ktor.
- Админские клики: 30мин/24ч/7д через минутную историю V28, 31/49/85 точек; анализ подозрительности по-прежнему30мин. Неизвестная история — null, а не0. V30 выносит дату начала сбора в runtime-readable metadata; Flyway-история остаётся закрыта, запись metadata у runtime запрещена.
- `features/updates/app-lifecycle*`: compiled buildID сравнивается с `/app-status.json`, опрос15сек при видимости + focus/online, maintenance-экран, уведомление2.5сек и повторная проверка перед reload. Защита от циклов (sessionStorage+URL), ошибок сети, поздних ответов и потери неперсистентной очереди/черновика. При ошибке сохранения — неблокирующий баннер для восстановления/копирования.
- `scripts/app-build.mjs` оборачивает обе сборки и создаёт ignored public/app-status.json с ID, встроенным в JS. Next/Vite no-store, SW пропускает статус. `deploy-update.sh` под flock: build+проверенный backup, runtime-статус и503gate Caddy, stop старых API/web, migrations/start/readiness/buildID, reopening. Ошибка после gate оставляет техработы. Runtime-файлы ignored в deploy/runtime. Деплой не выполнялся.
- Первый выпуск механизма требует однократного обновления прежних вкладок: код без lifecycle нельзя заставить исполнять новый протокол. Dev не перезагружается по buildID. Next/Sites в одном checkout делят generated public/app-status.json: для ручного запуска используйте комплект последней соответствующей сборки; Docker содержит собственный согласованный комплект.
- Документация: `docs/development/feedback.md`, `docs/operations/client-updates.md`, изменения админки/модерации/эксплуатации, README/карта файлов и игровая справка. Проверены253 относительные ссылки на файлы.
- Локальные проверки: полный `npm test`624/624, Sites и Next/VPS сборки последовательно, typecheck/lint/diff-check. После последних небольших правок формы повторены feedback+beta11/11 и ESLint. Добавлены Ktor/PostgreSQL тесты, обязательность feedback suite и maintenance smoke в CI.
- Ограничения проверки: здесь JDK17, нет Docker; Gradle9.6 не загрузился (Network is unreachable), нужныJDK25/PostgreSQL. После push проверить GitHub CI и устранить ошибки перед завершением. Реальный визуальный тест не подтверждён: cloud browser ранее блокировал localhost. Предыдущий commit9184744 полностью прошёл GitHub CI, это не проверка нового этапа.
