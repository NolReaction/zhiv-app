# Текущее состояние работы

- Репозиторий `NolReaction/zhiv-app`. Рабочая ветка — `work/0.6.6`, создана от `origin/master` (`990368539b00ff1a29e0017af18241c5524d5dd3`) и публикуется на GitHub по запросу пользователя. Здесь продолжается разработка следующей версии; master и сервер не изменять без задания. Если локальная ветка уже создана через `--no-track`, после fetch назначьте upstream `origin/work/0.6.6` и выполните `git pull --ff-only`.
- `feature/mochlik-tiled-world` остаётся на `bb52823ff6b7a1175b86d59f4d0af4a4470f8089` как история предыдущего этапа. Её изменения уже вошли в master одним релизным коммитом `c8d3dcc`.
- Пользователь успешно установил 0.6.5 с исправлением `9903685`: в присланном логе `Update complete`, главная HTTP 200, API ready, maintenance false. Исправление передаёт ID сборки через явный Docker build-arg, чтобы sudo не очищал его. Нового деплоя в рамках открытия ветки нет.
- DEV-панель полностью сохранена в master и этой ветке; переносить её обратно не требуется. Запуск `npm run dev:local` включает панель на главном экране и карте, overrides погоды/освещения, анимаций, птиц, камеры, облика и построек. `/prototype/tiled-world` доступен только в development. Тестовая выдача ресурсов — только локальный memory API, не production Ktor. Локальные данные могут исчезнуть после перезапуска процесса.
- Для разметки запускайте `npm run world:watch` во втором терминале; источник — `world/tiled/forest.tmj`. Навигация по коду и документации — `README.md` и `docs/repository-guide.md`.
- Номер пакета, Ktor и опубликованная новость остаются 0.6.5 до подготовки следующего релиза. Новые путешествия, рыбалка, строительство и крафт пока закрыты согласно `features/world/presentation.ts`; старый прогресс сохранён.
- Камера большой карты получила вертикальный запас по измеренной высоте верхнего/нижнего HUD (включая safe-area). При отдалении запас плавно исчезает до масштаба заполнения высоты; X и полный обзор прежние. `camera.ts` задаёт математику, `map-engine.ts` применяет её к жестам/кнопкам/клавиатуре и пересчитывает границы через ResizeObserver. Краткая инструкция — `docs/game/world-foundation.md`, подсказка есть в игровой справке.
- Проверено: 52/52 профильных теста камеры, движка, загрузки и UI; TypeScript и ESLint затронутых файлов. Браузер среды блокирует localhost (`ERR_BLOCKED_BY_CLIENT`), поэтому визуальная проверка на настоящем телефоне не выполнена. Проверить вручную: приблизить и потянуть оба вертикальных края из-под панелей, отдалить, повернуть телефон.
- Следующий этап — отдельные декоративные продолжения карты сверху и снизу по заданию пользователя. Пока изображений нет: прежние поля полного обзора квадратной карты на вытянутом экране сохраняются, дополнительного перемещения в этих полях нет. Разметка Tiled, круг и серверные данные не менялись.
