# Текущее состояние работы

- Репозиторий `NolReaction/zhiv-app`. Публикация только в `feature/mochlik-tiled-world`; `master` и деплой не трогать. База этапа `dfa2b62` (атмосфера и контактные тени поверх пользовательской v8-green).
- Текущий этап: готова dev-панель для круга и большой карты. Рабочий checkout `zhiv-app-dev-panel`, ветка `work/world-dev-panel`. Проверки завершены; этап подготовлен к публикации.
- В `features/world/dev/`: панель, стили и общий временный store. Кнопка DEV появляется после входа при `npm run dev:local` / `dev:lan`; production не включает панель и подписки на ручные команды. Настройки сбрасываются при перезагрузке, смене аккаунта или «Сбросить вид»; localStorage не используется.
- Управление: ясность/облака/морось/дождь/ливень; день/сумерки/ночь; бабочки, светлячки, птицы и немедленный пролёт; 25 поз (разово/повтор), направление, масштаб, примерка; уровни из каталога Tiled, тени, видимость, разметка; камера, ссылки на действующие игровые панели. Автосворачивание и «Повторить» оставляют сцену видимой на телефоне.
- Ресурсы: только существующая `dev_grant_resources` (+50 каждого) при development + `snapshot.devTools`. Это изменение локального баланса, отдельно от визуального предпросмотра. Повтор неопределённого запроса использует существующий механизм `world.retry`. Новых серверных команд/обходов админ-доступа нет.
- `new-map-scene.ts` и `map-engine.ts` применяют overrides, останавливают оба цикла анимации при паузе/скрытии, очищают подписки. Смена уровня атомарно подгружает только выбранную графику, отбрасывает старые ответы и сохраняет предыдущий рисунок при ошибке.
- `forest-atmosphere.ts`: ручные уровни дождя отличаются числом/длиной/скоростью капель; автоматический цикл сохранён. Живность немного заметнее в обоих режимах. reduced motion оставляет статичную сцену. `grounding.ts` / `tiled/renderer.ts` добавляют выключатели теней для сравнения.
- Изображения, геометрия и экспорт Tiled не менялись. Источник координат: `world/tiled/forest.tmj` → `features/world/tiled/forest.generated.json`. `world:watch` нужен только при изменениях в Tiled; графику экспортирует пользователь вручную. Инструкция: `docs/game/tiled-editor.md`.
- Ограничения перестройки сохраняются: реальные улучшения зданий, новые путешествия, вода, фонари и старый декор автоматически не включены. Прогресс игроков не меняется визуальными настройками.
- Проверки текущего кода: Sites и Next/VPS build, typecheck, lint, world:check, git diff --check — успешно. Весь веб-набор проверен группами: 279 + 124 + 4 (watch) + 58 (экспорт) = 465/465, без пропусков/отмен. Общий запуск не выдавал итоговую сводку, поэтому результаты подтверждены отдельными прогонами. SSR-проверка DEV: кнопка видна, inactive скрывает панель, рендер не вызывает выдачу.
- Визуальная проверка: нативный Canvas показал различимые уровни дождя, одинаковый вид живности при auto/on, целую исходную графику. Браузер среды блокирует localhost (`ERR_BLOCKED_BY_CLIENT`); ручной прогон панели/плавности на телефоне не выполнен.
- Следующий шаг после публикации: пользователь проверяет DEV локально; новые рисунки уровней добавляются через Tiled и автоматически становятся доступны для предпросмотра. Не включать игровую прогрессию без отдельной задачи.

## Восстановление после обрыва

Прочитать `AGENTS.md`, этот файл и нужный раздел `docs/repository-guide.md`. Проверить `git status`, текущую ветку, последние коммиты и remote. Незавершённый этап закончить и опубликовать в согласованную ветку; не повторять общий аудит и не менять графику.
