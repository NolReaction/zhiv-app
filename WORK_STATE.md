# Текущее состояние работы

- Репозиторий `NolReaction/zhiv-app`, ветка `feature/mochlik-tiled-world`, продолжение `8afba19`. Текущий этап — базовое ночное освещение. Master/деплой не трогать.
- Остались день и ночь (автоматически 19:00–07:00 по профилю), с плавным переходом. Из DEV и расписания удалены отдельные сумерки/облачность и прежняя плоская заливка. Дождь и его анимация сохранены.
- Новый `forest-lighting.ts`: холодное лунное освещение через multiply, тёплые локальные пятна, фонарь, факел с деревянной стойкой/пламенем, тип `glow`. Герой и обычная живность получают свет сцены; светлячки и огонь рисуются поверх. Пауза/reduced motion останавливают мерцание. Общий проход используется кругом, картой и Tiled preview; в preview огонь анимируется и без движения героя.
- Tiled: `Lights` — слой объектов либо группа с вложенными объектными слоями. Именованные Point, координаты центра пламени/лампы, свойства `kind`, `radius`, `intensity`, `color`, `flicker`; `role` не нужен. Экспорт/проверки/world:watch поддерживают изменения и последний корректный результат при ошибке. Инструкция с типами и примерами — `docs/game/tiled-editor.md`.
- В `forest.tmj` добавлен только слой Lights: домашний фонарь (678.19,624.76; radius135), факелы у поляны (489,618), дороги к реке (922,807), северной развилки (937,249). Все прежние слои и геометрия экспорта сравнены с HEAD и сохранены точно; графика не менялась. При перемещении дома переносить home-lantern вместе с ним. Точки света внутри скрытого здания отключаются вместе с ним.
- Освещение кешируется в текстуре максимум 768px, инвалидируется координатами/цветом/радиусом/яркостью и составом источников; мерцание не пересобирает её. Независимый native CPU Canvas: свет+эмиттеры ~1.01 мс, полный ясный ночной кадр ~1.27 мс при700px. Это не браузер/телефон. Круг/мир и preview совпадают, проверены скрытие источников, дневной режим, пауза и reduced motion; дневные/ночные рендеры просмотрены.
- Проверки: полный `npm test` — 573/573; после финальной поправки отсечения мерцающего ореола и статического flicker=0 повторены 14 световых/preview тестов. Typecheck, lint, world:check прошли. Финальные Sites и Next/VPS сборки прошли последовательно.
- Ограничение этой базы: нет расчёта перекрытия света деревьями и физических теней от новых контуров. Далее — пользовательская оценка ночи и размещение Lights через Tiled, затем отдельная проработка препятствий света при необходимости. Прежние подробные Water/WaterExclusions, распределённый дождь и шесть сценариев птиц сохранены.
