# Текущее состояние работы

- Подготовка релиза **0.6.5**: один коммит поверх `master` (`167da037866885570388137c44d374ae41fc5b9d`), состояние рабочей ветки `feature/mochlik-tiled-world` — `bb52823ff6b7a1175b86d59f4d0af4a4470f8089`. Рабочая ветка сохраняется отдельно. Пользователь явно разрешил публикацию в master; развёртывание VPS выполняет сам по выданной команде.
- Web, lockfile и Ktor имеют версию 0.6.5. В `public/updates.json` первая пользовательская новость: лес, ночь, дождь/обитатели, справка, уровни, новости/обращения и обновление клиента. Обозначены закрытые занятия нового мира; коллекции также объясняют ограничения.
- Production: DEV-панели/overrides выключены; `/prototype/tiled-world` отдаёт 404; API в памяти доступен только development/test, переменная `ENABLE_DEV_API` больше не открывает его в production. Штатная админка защищена серверной сессией/allowlist, тестовая выдача ресурсов в Ktor не реализована. Локальная разработка сохраняет DEV.
- Миграции V1–V28 побайтово совпадают с прежним master и указанным пользователем выпуском `be31b6e`. V29 добавляет feedback, V30 — служебную дату сбора нажатий. Сброс данных не нужен. Добавлен обязательный PostgreSQL-тест обновления заполненной V28-базы с сохранением старых42таблиц, сессии, игровых данных и квитанций.
- `deploy-update.sh` собирает релиз, создаёт читаемый backup, включает maintenance-gate, останавливает старые API/web, применяет миграции, проверяет readiness/buildId и открывает доступ. Первый переход со старого клиента требует однократного ручного refresh; следующие автоматические. Реальный VPS не изменялся.
- Документация релиза: `docs/releases/release-0.6.5.md`; инструкции по обновлениям и dev-доступу актуализированы. Полный результат проверок и публикации подтверждается CI релизного SHA; предыдущие проверки рабочей ветки не заменяют его.
- Релизный checkout прошёл **628/628 веб-тестов**, typecheck/lint, Sites и Next/VPS сборки, world:check и645проверок относительных ссылок. Production HTTP: главная200, одна новость0.6.5/no-store, совпадениеbuildId/no-store, prototype404, memoryAPI503 даже сENABLE_DEV_API=true. Дополнительно5/5deploy-тестов, проверкаshell/JS/YAML иread-onlyreviewмиграций/авторизации. Ktor/PostgreSQL/Compose обязательны в CI релизного SHA до fast-forward master на этот же единственный коммит. Локально JDK17/безDocker; реальный VPS не изменён.
