# Текущее состояние работы

- Репозиторий `NolReaction/zhiv-app`, ветка `feature/mochlik-tiled-world`, продолжение `50e5bf3`. Этап: новости в beta-test и компактные уровни. Master/деплой не трогать; пользователь одобрил текущую ночь, графика и игровые пороги в этом этапе сохранены.
- `features/check-in/beta-info.tsx` и новый CSS: кнопка с наследуемым шрифтом, иконкой, числом непрочитанных записей; сверху окна закреплено прежнее сообщение о сотрудничестве, ниже отдельная прокрутка версий. По открытию читается только последняя раскрытая запись, остальные — по раскрытию. Фокус идёт на заголовок, возвращается к кнопке. Beta/уровень имеют область нажатия 44px.
- Лента `public/updates.json` (schemaVersion1), одна начальная запись пакета0.6.3. Для публикации добавляется новый стабильный ID; версия может повторяться. Инструкция: `docs/development/updates-and-levels.md`, ссылки добавлены в README/оглавление/путеводитель. Новость на сервере появляется после развёртывания JSON вместе с нужным кодом.
- `features/updates/release-notes.ts` валидирует записи, `release-notes-store.ts` получает их с сервера, `use-release-notes.ts` подключает React. Mount/open/focus/online/visibility и каждые5мин видимой вкладки; защита от повторных запросов, timeout10сек, отмена и игнорирование поздних ответов. Ошибки оставляют корректную ленту; перезагрузки и сброса тапов нет.
- Прочитанные ID — localStorage отдельно по publicId, с объединением между вкладками и памятью при запрете хранилища. Это НЕ синхронизация между устройствами. Новая запись в открытом окне остаётся непрочитанной до раскрытия. Конкурентные записи вкладок восстанавливают объединение на диске.
- `/updates.json` обслуживается web, не Ktor. `next.config.ts` и `public/_headers` дают no-store; Service Worker пропускает запрос. Sites содержит актуальные JSON/headers; локальная HTTP-проверка production Next вернула200, JSON и Cache-Control:no-store.
- `game-levels-button.tsx`/CSS: группы1–10,…,91–100, текущая группа при каждом открытии; точная полоса до следующего уровня, максимум100; десять реальных этапов значков1/10/20/30/40/50/60/75/90/100, закрытые можно посмотреть. Сами пороги/начисления не менялись. Игровая справка обновлена.
- Проверки: полный `npm test`595/595; Sites и Next/VPS сборки последовательно; typecheck/lint/diff-check прошли. После отвязки тестов ленты от конкретной версии/числа записей повторены10/10. Направленные тесты включают границы уровней, прочтение записей, ошибки сети/хранилища, две вкладки и отмену запросов. Проверены209 относительных ссылок/якорей изменённых документов.
- Ограничение: доступный cloud browser ранее отклонял localhost с ERR_BLOCKED_BY_CLIENT; живой визуальный тест на телефоне не подтверждён. SSR/проверку CSS не выдавать за браузерный рендер. Следующий шаг — пользовательская оценка двух меню; новые новости добавлять по инструкции, прошлые ID не переименовывать.
