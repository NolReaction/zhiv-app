# Текущее состояние работы

- Репозиторий `NolReaction/zhiv-app`. Публикация только `feature/mochlik-tiled-world`; master/деплой/графику/Tiled не менять.
- Завершён следующий этап после `afe39df`: дождь/поверхности воды и живые птицы. Дождь — короткие полупрозрачные капли трёх слоёв с ветром; лужи — влажный край, отражения, всплески и двойные расходящиеся круги.
- `forest-water.ts` рисует блики течения и дождевые удары на обоих рукавах реки. `forest-water-layout.json` хранит проверенную маску открытой воды: берег, причал, камни и растительность исключены. Эффекты вне камеры не рисуются.
- `forest-birds.ts` задаёт полёт/планирование, плавную посадку, повороты головы, чистку перьев, взмах хвоста, прыжок и взлёт. Автоматические визиты редкие, охватывают карту. DEV «Пролёт и посадка птиц» запускает полный цикл 34 с.
- Маска реки и точки посадки привязаны к фону `fedcfbd622df`; при смене рисунка их надо сверить/обновить. Неизвестная карта не наследует привязанные эффекты. Лужи остаются на проверенной площадке возле spawn.
- Обе камеры используют общие активные часы: elapsed для анимаций, timestamp для расписания погоды. Пауза/фон/reduced motion согласованы. Существующие сценки Мохлика и открытая DEV сохранены.
- Проверки 23.09.2026: 501/501 веб-тест, typecheck, lint, сборки Sites и Next/VPS, git diff --check. Просмотрены дневные/ночные рендеры воды и стадии посадки/отдыха/взлёта. Локальный браузерный предпросмотр недоступен; визуальная проверка выполнена через рендерер Canvas. Независимая проверка кода блокеров не выявила; CI API не запускался.

## Восстановление

Прочитать AGENTS.md и этот файл, проверить git status/log/remote. Этап завершён; следующий шаг — пользовательская проверка через DEV при `npm run dev:local` / `dev:lan`. Не повторять реализацию или общий аудит без нового задания.
