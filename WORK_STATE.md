# Текущее состояние работы

- Текущая разработка — `work/0.6.7`, создана от `master` на релизе `97c11e0` (0.6.6). Новые задачи и коммиты выполнять здесь; `work/0.6.6` сохранена как история. DEV уже входит в базу и включается через `npm run dev:local`; отдельно можно запустить `npm run world:watch`. Версии и новости следующего релиза обновлять при подготовке выпуска.
- Выпуск **0.6.6**: состояние `work/0.6.6` на `dbc1e5a` подготовлено одним squash-коммитом поверх `master` (`9903685`). История рабочей ветки сохранена; следующие изменения мира продолжаем в согласованной рабочей ветке, не в master. Публикация в Git не означает деплой на VPS.
- Версии web/API — 0.6.6. Пользовательская новость добавлена первой в `public/updates.json`, запись 0.6.5 сохранена. Состав выпуска и обновление сервера — `docs/releases/release-0.6.6.md`.
- Новых миграций нет: V1–V30, backend, экономика и deploy-скрипт прежние; исправление передачи APP_BUILD_ID через sudo сохранено. DEV только development; production prototype и memory API закрыты. Новые дальние путешествия, рыбалка, строительство и крафт пока отключены через `presentation.ts`.
- Utility AI в `work/0.6.7`: энергия, любопытство, уют и внимание влияют на занятия; одно намерение держится до завершения/прерывания, память учитывает повторы и неудачи. После тапа 30 секунд бодрствования. Дом выбирается после 180 секунд видимого неактива либо раньше при усталости/сырости. Круг и карта используют одну сессию; пауза, фон и reduced motion не расходуют потребности.
- Статус Мохлика под кругом и в HUD карты открывает «Как Мохлик?»: реальное занятие, настроение, словесные показатели и состояние памяти. DEV → «Мышление и память» показывает оценки, причины недоступности, журнал и выгружает ограниченный JSON без данных аккаунта. Документация — `docs/game/world-mind.md`, правила также в игровой справке.
- `forest-memory.ts` сохраняет потребности, недавние занятия, безопасное положение/сон дома и рост грибов в localStorage по аккаунту. Нет офлайн-начислений и синхронизации декоративной жизни между устройствами/вкладками. При изменении геометрии старая позиция отбрасывается. Принудительные DEV-сценарии сохраняют чистый снимок и отключают дальнейшую запись до конца сессии; просмотр диагностики и камера её не отключают.
- `navigation.ts` отвечает за проходимость и поиск; `steering.ts` готовит проверенные закругления и профиль торможения; `clearing-activity.ts` исполняет движения; `interaction-navigation.ts` проверяет локальные входы; `forest-director.ts` связывает занятия, героя и обитателей. Справочник — `docs/game/world-navigation.md`.
- Источник разметки — `world/tiled/forest.tmj`, пользовательские полигоны сохранены. Экспорт — `npm run world:export`, сверка — `npm run world:check`. Не править generated JSON вручную. Редактирование: `docs/game/tiled-quickstart.md` и `docs/game/tiled-editor.md`.
- Routes для свободной полянки необязателен; слой сохранён для старых карт, прототипа и DEV «Прежние маршруты». Основные данные — WalkAreas, Obstacles, PointsOfInterest и входы дома/кустов. Скрытые слои экспортёр отвергает; блокировка слоя безопасна.
- Фауна имеет постоянные ID и физически прилетает к герою: бабочки днём, светлячки ночью; clearing вычитается из forest через excludeHabitatId. Куст перекрывает героя листвой, шевелится и роняет ягоды; грибы/кусты/свет/вода редактируются в Tiled.
- Камера принята; дорисовка границ отложена. Следующие отдельные задачи: составные поручения/GOAP, дальние поездки и полное сохранение популяции. Оптимизация по устройствам в этом этапе не выполнялась по просьбе пользователя. Tiled, изображения, версии/новости, сервер, БД и экономика не менялись.
- Проверки Utility AI: TypeScript, ESLint, world:check, сборки Sites и VPS прошли. Полный `npm test`: 959/959; после исправления дублей кандидатов и добавления регрессии повторены актуальная сборка Sites и 80/80 связанных тестов. Всего теперь 960 тестов. Diff и ссылки документации проверены.
- Ограничение проверки UI: локальный dev-сервер запущен и остановлен; доступный браузер блокирует localhost, поэтому живой проход узкого/широкого экрана и клавиатуры не подтверждён. SSR/UI-тесты прошли. Физический телефон, VPS и API/Compose локально не проверялись; публикация коммита не означает деплой.
