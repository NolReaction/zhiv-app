# Текущее состояние работы

- Репозиторий: `NolReaction/zhiv-app`.
- Рабочая ветка: `feature/mochlik-tiled-world`; `master` не изменяем.
- Задача: безопасная уборка лишней документации и доказанно неиспользуемого кода с поэтапными коммитами.
- Исходная точка: `5a117b2` — завершена уборка изображений, семь пар PNG/WebP. На ней прошли 376 веб-тестов, обе сборки, Tiled и браузерные проверки.
- Сохранён этап `cc9d120`: правила коротких задач и восстановление после обрыва.
- Документация очищена: удалены 22 старых отчёта/спецификации; архив доступен в Git на `5a117b2`. Перенесены правила `umask` и смены домена; текущие runbooks и инструкция восстановления старого VPS сохранены. Ссылки и Bash-примеры проверены.
- Аудит кода завершён: целых неиспользуемых модулей не найдено. Подготовлены отдельные рабочие правки: удаление трёх мёртвых объявлений, 17 CSS-классов и лишнего прямого объявления `date-fns` (остаётся зависимостью календаря).
- Следующий шаг: дождаться проверок кода, проверить сохранность оставшихся CSS-правил, сохранить второй этап с результатами и опубликовать в той же ветке. Кодовые правки пока не входят в коммит документации.
- Ограничения: не менять графику, `grid-world`, миграции, данные пользователей, действующие runbooks и два контура сборки. Сомнительные файлы сохранять.
- Проверки: typecheck прошёл; lint и полный набор веб-тестов запущены. Итоговые результаты должны быть записаны после завершения, прежний прогон не заменяет их.

## Как продолжить после обрыва

1. Прочитать `AGENTS.md` и этот файл.
2. Проверить текущую ветку, `git status --short` и `git log -5 --oneline`; сравнить с удалённой веткой перед публикацией.
3. Продолжить со следующего незавершённого шага, сохранив пользовательские изменения. Не повторять уже завершённый аудит без новой причины.
