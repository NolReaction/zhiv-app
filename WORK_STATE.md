# Текущее состояние работы

- Репозиторий: `NolReaction/zhiv-app`.
- Рабочая ветка: `feature/mochlik-tiled-world`; `master` не изменяем.
- Статус: безопасная уборка завершена; изменения сохранены поэтапными коммитами в рабочей ветке. Деплой и слияние в `master` не выполнялись.
- Исходная точка: `5a117b2` — завершена уборка изображений, семь пар PNG/WebP. На ней прошли 376 веб-тестов, обе сборки, Tiled и браузерные проверки.
- Сохранён этап `cc9d120`: правила коротких задач и восстановление после обрыва.
- Этап `c6501fd`: удалены 22 старых отчёта/спецификации; архив доступен в Git на `5a117b2`. Перенесены правила `umask` и смены домена; текущие runbooks и инструкция восстановления старого VPS сохранены.
- Этот этап: удалены `containsPoint`, `resumeAccountAction`, `CHECK_IN_COOLDOWN_MS`, 17 неиспользуемых CSS-классов и лишнее прямое объявление `date-fns`. Пакет остаётся зависимостью календаря; версии всех установленных зависимостей не менялись.
- Целых неиспользуемых модулей не найдено. Сохранены реально используемые UI-компоненты, Kotlin API, тесты, скрипты обслуживания, Sites/VPS и лицензии. Изображения и их ручной экспорт не менялись.
- Проверки: typecheck, lint, обе сборки; все 47 файлов веб-тестов прошли двумя последовательными группами (208 + 168 = 376 тестов, без ошибок/пропусков). Подтверждена неизменность 936 оставшихся CSS-селекторов, их свойств и порядка. Проверены 175 локальных Markdown-ссылок и Bash-примеры изменённого runbook. Backend-тесты заново не запускались: backend не изменён.
- Следующий шаг: обновить локальную рабочую ветку и продолжить редактирование PNG по `art/README.md` либо взять следующую отдельную задачу. Повторная общая уборка сейчас не требуется.
- Ограничения: не менять графику, `grid-world`, миграции, данные пользователей, действующие runbooks и два контура сборки. Сомнительные файлы сохранять.

## Как продолжить после обрыва

1. Прочитать `AGENTS.md` и этот файл.
2. Проверить текущую ветку, `git status --short` и `git log -5 --oneline`; сравнить с удалённой веткой перед публикацией.
3. Продолжить со следующего незавершённого шага, сохранив пользовательские изменения. Не повторять уже завершённый аудит без новой причины.
