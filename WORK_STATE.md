# Текущее состояние работы

- Репозиторий: `NolReaction/zhiv-app`; ветка `feature/mochlik-tiled-world`. `master` не менять, не деплоить.
- База этапа: `7ca2db9` — пользователь заменил карту и удалил старые изображения.
- Этап 1 реализован: чистая новая карта в основном мире, круге Мохлика и Tiled-прототипе. Старые объекты, маршруты, эффекты и подарочный декор временно отключены через `features/world/presentation.ts`.
- Исходник: `art/world/prototype/forest-ground.png`, 2560×2560. PNG не изменять. Единственная браузерная версия: `public/world/prototype/forest-ground.webp`, экспорт без потерь. Логический размер мира остаётся 1254×1254.
- Сохранить данные игроков, начисление наград, инвентарь и получение награды за уже начатые путешествия. Новое строительство, прогулки и создание вещей до следующего этапа недоступны.
- Не трогать `grid-world`, backend, миграции и инфраструктуру. Старые алгоритмы пока сохраняются для дальнейшей адаптации.
- Проверки: typecheck, lint, Sites и Next/VPS build, world:check; 386 веб-тестов прошли без ошибок и пропусков. PNG проверен на неизменность, WebP сохраняет исходные пиксели.
- Браузерная проверка ограничена: доступ облачного браузера к локальному стенду заблокирован. Внешний вид и управление на реальном телефоне проверить локально после получения коммита.
- Публикация: один коммит только в рабочую ветку; без PR, слияния и деплоя.
- Далее: разметить новые места, коллизии и маршруты, затем подключать здания отдельными согласованными участками с тенями. Это отдельный этап.

## Восстановление после обрыва

1. Прочитать `AGENTS.md` и этот файл.
2. Проверить ветку, `git status --short`, `git log -3 --oneline` и удалённую ветку.
3. Закончить текущий этап и проверки; не повторять общий аудит и не переписывать рабочие системы.
