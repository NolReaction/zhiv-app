# Текущее состояние работы

- Репозиторий `NolReaction/zhiv-app`, ветка `work/0.6.6`. База текущего этапа — `586072d` (согласованный проект живого леса), предыдущий код куста — `7682410`, пользовательская геометрия — `1671070`. Публиковать в этой ветке; master, сервер, БД, номер 0.6.5 и `updates.json` не менять.
- Пользователь одобрил полное внедрение проекта `docs/game/living-world-plan.md`: непрерывная жизнь насекомых, настоящие встречи, свободная полянка, выбор занятий, Tiled и DEV-диагностика. Камера принята; дорисовка карты сверху/снизу отложена. Новые путешествия/рыбалка/стройка/крафт по-прежнему закрыты через `presentation.ts`.
- Реализованы `forest-fauna.ts` и `hero-anchors.ts`: постоянные ID/облик/координаты, территории и растения, подлёт к фактической лапе, контакт по расстоянию/скорости, физический отлёт и возвращение. Один участник рисуется один раз. Тап освобождает героя за ≤0.6 с, насекомое продолжает полёт. День/дождь ведут в укрытие; дневной светлячок сохраняет тело без ночного свечения.
- `forest-director.ts` соединяет насекомых, предметы и `clearing-activity.ts`: реальные кандидаты, подход к выросшему грибу/листику, спокойные паузы, память повторов, редкое посещение куста. Недоступные цели отвергаются. Предмет нельзя подтянуть издали после переключения DEV-режима. Сон/куст безопасно завершаются перед новым запросом.
- `navigation.ts`: кешированная небольшая сетка, ограниченный A*, проверка движущейся опоры, запрет срезать углы/воду/коллизии, безопасное сокращение пути. `forest-behavior.ts` выбирает точки интереса и свободную траву с учётом погоды/отдыха/повторов. Сетка не рисуется игроку и не задаёт пошаговую ходьбу.
- Tiled получил WalkAreas, Obstacles, PointsOfInterest, Habitats, WildlifeAnchors; источник `world/tiled/forest.tmj`, экспорт только `npm run world:export`. Имеющаяся вода/свет/дом/63-точечный контур куста/гриб и старые маршруты сохранены. Новую начальную разметку травы и растений можно уточнять вручную; сложные маршруты по миру не открыты.
- Дом и куст сохраняют свои точные переходы по авторским линиям. 180 секунд видимой активности без внимания → безопасный возврат → сон дома; автозанятия не сбрасывают этот срок. Куст перекрывает непрозрачного героя листвой, бросает декоративные ягоды/листья. Локальные шаги/шорох тревожат ближайших насекомых и привлекают внимание сидящей птицы (`forest-bird-reactions.ts`).
- `forest-session.ts` хранит всех участников/координатор/птиц; круг и карта используют один шаг времени. Pause/background/reduced motion останавливают новые движения. Рендер не продвигает симуляцию. Обе камеры получают одну позу, одни ID и позиции.
- DEV только development: новые независимые навигация/обитатели, пути/цели/ID/фаза встречи/причина отказа. Режим «Авторские маршруты» сохраняет сравнение с прежней ходьбой через безопасный возврат. Кнопки насекомых запрашивают существующую особь, не создают дубля.
- Локально: `npm ci`, `npm run dev:local`; во втором терминале `npm run world:watch`. Переключатели DEV не пишут в серверную экономику. Локальный memory API может очиститься после перезапуска.
- Проверено: `npm test` — Sites-сборка и 867/867 тестов; `npm run build:vps`, TypeScript, полный ESLint, `world:check`, diff и 235 ссылок документации прошли. Просмотрены реальные Canvas-кадры подлёта/контакта/отлёта днём и ночью. Проверены общая сессия камер, тап/повторные тапы, пауза/reduced motion, статическое ночное свечение, подход к предмету, DEV-смена навигации, сон и безопасный выход из дома/куста. После полного прогона добавлена ещё регрессия выхода из дома к насекомому; 16/16 тестов координатора прошли. Физический телефон пока не проверен.
- Результат: основа согласованного живого леса публикуется в `work/0.6.6`; 3 бабочки/5 светлячков возле дома и 3/7 в остальном лесу, без увеличения общей популяции. Инструкции новых слоёв — `docs/game/tiled-editor.md`; владельцы состояния и следующие этапы — `docs/game/living-world-plan.md`. Далее — ручная визуальная настройка проходов/растений при необходимости; дальние переходы, новые поездки и сохранение фауны после закрытия приложения остаются отдельными задачами.
- Документация Tiled после `bf62956`: добавлена `docs/game/tiled-quickstart.md` — назначение 15 слоёв, безопасное упрощение вида редактора, пошаговая настройка земли/занятий/посадок/куста/грибов и диагностика. Ссылки добавлены в README, указатель и карту исходников; в полном справочнике исправлена клавиша вершин E → O. Сверены текущие объекты/свойства и названия DEV, 239 локальных ссылок/якорей и `git diff --check` прошли. Это этап только документации; карта, код и игровой экспорт не изменены, сборки повторно не запускались. Следующий шаг — ручная настройка пользователем `home-clearing`, точек занятий и посадок по памятке.
