# Текущее состояние работы

- Репозиторий `NolReaction/zhiv-app`. Рабочая ветка — `work/0.6.6`, создана от `origin/master` (`990368539b00ff1a29e0017af18241c5524d5dd3`) и публикуется на GitHub по запросу пользователя. Здесь продолжается разработка следующей версии; master и сервер не изменять без задания. Если локальная ветка уже создана через `--no-track`, после fetch назначьте upstream `origin/work/0.6.6` и выполните `git pull --ff-only`.
- `feature/mochlik-tiled-world` остаётся на `bb52823ff6b7a1175b86d59f4d0af4a4470f8089` как история предыдущего этапа. Её изменения уже вошли в master одним релизным коммитом `c8d3dcc`.
- Пользователь успешно установил 0.6.5 с исправлением `9903685`: в присланном логе `Update complete`, главная HTTP 200, API ready, maintenance false. Исправление передаёт ID сборки через явный Docker build-arg, чтобы sudo не очищал его. Нового деплоя в рамках открытия ветки нет.
- DEV-панель полностью сохранена в master и этой ветке; переносить её обратно не требуется. Запуск `npm run dev:local` включает панель на главном экране и карте, overrides погоды/освещения, анимаций, птиц, камеры, облика и построек. `/prototype/tiled-world` доступен только в development. Тестовая выдача ресурсов — только локальный memory API, не production Ktor. Локальные данные могут исчезнуть после перезапуска процесса.
- Для разметки запускайте `npm run world:watch` во втором терминале; источник — `world/tiled/forest.tmj`. Навигация по коду и документации — `README.md` и `docs/repository-guide.md`.
- Номер пакета, Ktor и опубликованная новость остаются 0.6.5 до подготовки следующего релиза. Новые путешествия, рыбалка, строительство и крафт пока закрыты согласно `features/world/presentation.ts`; старый прогресс сохранён.
- Камера большой карты получила вертикальный запас по измеренной высоте верхнего/нижнего HUD (включая safe-area). При отдалении запас плавно исчезает до масштаба заполнения высоты; X и полный обзор прежние. `camera.ts` задаёт математику, `map-engine.ts` применяет её к жестам/кнопкам/клавиатуре и пересчитывает границы через ResizeObserver. Краткая инструкция — `docs/game/world-foundation.md`, подсказка есть в игровой справке.
- Пользователь принял камеру; декоративные продолжения карты сверху и снизу отложены. Горизонтальное движение и геометрия камеры в текущей задаче не менялись.
- Мохлик гуляет только по полянке: три линии `clearing-look`, `clearing-sniff`, `clearing-rest`. Прерывания переработаны: нажатие завершает занятие, отменяет короткий сон, даёт пробуждение и 30 секунд бодрствования; повторные тапы не перезапускают ответ. Под дождём отдых заменяется уходом за шерстью. Варианты осмотра, обнюхивания и ухода чередуются.
- `clearing-activity.ts` также ведёт сон дома: 180 секунд видимого активного времени без взаимодействий → возврат по тропке → `home-approach` → физический переход от `home-entry` к `home-doorway` → скрытый сон. Тап по кругу/дому запускает выход, потягивание, приветствие и возврат. Общая `forest-session.ts` сохраняет положение между кругом и картой. База данных и серверные команды не участвуют.
- `forest-life.ts` добавляет листик: заметить, поднять, рассмотреть, вернуть. Нажатие запускает `interruptForestLife`: целый гриб/листик опускается, надкушенный гриб доедается, насекомое улетает; `pendingAttention` ждёт ≤0.6 с. Ручные позы не продвигают предметную сценку, reduced motion сохраняет предмет в лапах. Жёсткая DEV-отмена возвращает нетронутый гриб без дубликата.
- Tiled: новый `behavior: home` с `siteId: home`, необязательный маркер здания `role: doorway`. Добавлены только линия `home-approach` и точка `home-doorway (661.5, 632.5)`, прежняя вода/свет/дом/вход сохранены. Геометрия проверяется; исключение собственной коллизии ограничено порогом. Кусты и камни без коллизий нужно обходить вручную.
- DEV → «Лесные сценки»: «Отправиться спать домой», «Разбудить Мохлика», «Рассмотреть листик», диагностика маршрутов. Фиксированная поза DEV остаётся явным предпросмотром; обычное поведение включается через «Авто». Светлячки с предыдущего этапа сохранены.
- Документация: `docs/game/world-foundation.md` — состояния/прерывания/сон; `docs/game/tiled-editor.md` — редактирование маршрутов и порога; `world-atmosphere.md` — взаимодействия. Пользовательские правила добавлены в игровую справку.
- Новый этап: грибы полностью управляются точками `role: mushroom` в слое `Mushrooms`; текущий `mushroom-1` сохранён на прежнем месте. Имена — стабильные ID. Удаление всех точек убирает грибы; скрытых запасных позиций нет. Далёкие грибы декоративные, подбор только возле spawn, без пересечения воды/постройки. Рост и восстановление сохранены; записи в БД не добавлены.
- Куст слева от дома размечен в верхнем слое `Bushes`: контур `role: bush`, точки `bush-entry` / `bush-hide`, общий `bushId: clearing-bush`. `Routes → clearing-bush` имеет `behavior: clearing`, `activity: bush`, `bushId`; вход `(582,650)`, укрытие `(548,623)`. Остальная разметка воды/света/дома сохранена. Контур повторяет листву фона, не создаёт новый рисунок.
- `clearing-activity.ts`: подход → подготовка/приседание → прыжок → укрытие с шевелением → выход/отряхивание → возврат. Тап по занятому кусту/кругу вызывает выход перед приветствием, повторные тапы не перезапускают сцену. Сон сначала завершает выход. Явный DEV-запрос работает при выключенном авто, отмена не оставляет очередь куста. Ручная поза в кусте безопасно ставит героя у входа.
- `forest-bush-painter.ts` перекрывает героя точным контуром и шевелит внутреннюю листву; неподвижный край исключает двойную обводку, маленькая текстура кешируется для обеих камер. `grounding.ts` поддерживает `lift`: тело летит по дуге, тень остаётся на земле. Пауза и reduced motion сохраняют фазу без скрытого продвижения.
- DEV: «Спрятаться в кусте»; общая разметка показывает грибы, контур/вход/укрытие куста. Новый независимый переключатель «Границы воды» показывает поверхности голубым и исключения коралловым пунктиром поверх освещения. Path2D кешируются, маска воды не перестраивается. DEV по-прежнему только development.
- Инструкции расстановки и редактирования — `docs/game/tiled-editor.md`, разделы «Расставить грибы» и «Куст: контур, укрытие и подход». Ссылки/устройство обновлены в `world-foundation.md`, `world-atmosphere.md`, `docs/repository-guide.md`.
- Проверка текущего этапа: `npm test` — Sites-сборка и 750/750 тестов; `npm run build:vps`, TypeScript, полный ESLint прошли. Экспорт Tiled сверён; реальные Canvas-кадры подхода, прыжка, скрытия и выхода просмотрены. Интеграционные тесты проверили тап большой карты по скрытому герою, повторные тапы, обе камеры, паузу/reduced motion и независимость показа воды. На физическом телефоне не проверено.
- Следующий шаг — оценка сценки пользователем и ручная настройка Tiled. Версия/новость остаются 0.6.5; master, сервер и БД в этом этапе не менялись. После публикации коммита продолжать в `work/0.6.6`.
